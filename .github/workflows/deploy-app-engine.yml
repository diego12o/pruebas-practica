
name: learn-github-actions

on:
  pull_request:
    types:
      - closed
jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:

    - name: Deploy to App Engine
      id: deploy
      uses: google-github-actions/deploy-appengine@v0.2.0
      with:
        #project_id: "level-hope-372123"
        #credentials: ./level-hope-372123-4f28d5a1d841.json
        serviceAccountJsonPlainText: ./level-hope-372123-4f28d5a1d841.json
